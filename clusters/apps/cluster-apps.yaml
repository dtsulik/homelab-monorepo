apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-apps
  namespace: argocd
spec:
  generators:
  - matrix:
      generators:
      - git:
          repoURL: https://github.com/dtsulik/homelab-gitops.git
          revision: HEAD
          directories:
          - path: apps/*
      - list:
          elements:
          - cluster: homelab-vm
            url: https://kubernetes.default.svc
            values:
              project: default
  template:
    metadata:
      annotations:
        argocd-image-updater.argoproj.io/image-list: gif-doggo-apigw,gif-doggo-intake,gif-doggo-output,gif-doggo-process,gif-doggo-request,gif-doggo-status
      name: '{{path.basename}}-{{cluster}}'
    spec:
      syncPolicy:
        automated:
          prune: true
      project: '{{values.project}}'
      source:
        repoURL: https://github.com/dtsulik/homelab-gitops.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: '{{url}}'
        namespace: '{{path.basename}}'