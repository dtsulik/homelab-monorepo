apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-apps
  namespace: argocd
spec:
  generators:
  - matrix:
      generators:
      - git:
          repoURL: https://github.com/dtsulik/homelab-gitops.git
          revision: HEAD
          directories:
          - path: apps/*
      - list:
          elements:
          - cluster: homelab-vm
            url: https://kubernetes.default.svc
            values:
              project: default
  template:
    metadata:
      annotations:
        argocd-image-updater.argoproj.io/image-list: dtsulik/gif-doggo-apigw,dtsulik/gif-doggo-intake,dtsulik/gif-doggo-output,dtsulik/gif-doggo-process,dtsulik/gif-doggo-request,dtsulik/gif-doggo-status
        argocd-image-updater.argoproj.io/update-strategy: digest
      name: '{{path.basename}}-{{cluster}}'
    spec:
      syncPolicy:
        automated:
          prune: true
      project: '{{values.project}}'
      source:
        repoURL: https://github.com/dtsulik/homelab-gitops.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: '{{url}}'
        namespace: '{{path.basename}}'